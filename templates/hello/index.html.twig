{% extends 'base.html.twig' %}

{% block body %}
{# 👆 this now only renders inside of the body, everything else is extended from the base #}
<div>
    {# How data is conditionally rendered should be done inside the template #}
    {% if messages|length > 0 %}
        {% for key, message in messages|slice(0, limit) %}
        <div>
            <a href='{{ path('app_show_one', { id: key }) }}'>{{ message.message }}</a>
            {# 👆 generate URL for each using the key value & the twig 'path' function #}

        </div>
            {{ include('hello/_message.html.twig', {message: message}) }}
            {# 👆 the partial to include needs to be declared, the data being sent to the partial can also be declared here. Data is inherited by partials from parents but declaring it can change the names if necessary #}
        {% endfor %}
    {% else %}
        No messages...
    {% endif %}
</div>
{% endblock %}